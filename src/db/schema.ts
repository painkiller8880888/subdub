/**
 * P0-04 deliberately has no business tables yet.
 *
 * This object is the Drizzle schema entry point. Asset, terminology, and
 * improvement-log tables will be added by later migrations when their
 * requirements are defined.
 */
export const schema = {} as const;
